This fake node_modules directory is necessary so that the examples 
can locate dcp-client as though they were run from inside another 
project which had dcp-client as its dependency.

dcp-client is a symlink to the top of the repository. 

This is untested on Windows and might not work. /wg aug 2019
